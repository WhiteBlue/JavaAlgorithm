package sort.heap;

/**
 * 堆排序
 */
public class HeapSort {

    /**
     * 大顶堆
     *
     * @param array array
     * @param value insert
     */
    public void sort(int[] array) {
        int i = 0, head = 0;

        while (head < array.length) {

        }
    }

}

